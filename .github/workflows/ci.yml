name: CI
on:
  push:
  pull_request:
jobs:
  lint-and-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Wire actual install, lint, typecheck, test in next milestone"
